// Estricturta de una funcion

/**
 * Genera un string con camelCase a partir de un texto y un numero
 * @param {string} textoSaludo - El texto a convertir
 * @param {number} [numeroAsiento=1] - El numero a concatenar
 * @returns {string}
 */
function nombreEnCamelCase(textoSaludo, numeroAsiento = 1) {

    console.log(textoSaludo, numeroAsiento)

    return 'mensaje recibido'
}

nombreEnCamelCase('hola', 27)